下面给你的是 **4 份可直接复制给 AI 的高规格提示词**。它们不是工程文档本身，而是“用来生成工程文档”的提示词；我已经把你上一轮的拆分原则、模块边界、主干/增强分离、MVP/增强/远期、以及你上传数据中的表名、字段类别、时间边界和使用方式融进去了。整体遵循你那份拆分文档里的主线：**预测 → 组合 → 执行 → 增强**，并且显式要求 AI 生成能落到 Python 项目架构、接口、测试和稳健性设计上的工程文档，而不是空泛研究报告。你的数据里目前至少包含基础交易层 `raw_daily / raw_daily_basic / raw_moneyflow / raw_cyq_perf / raw_fina_indicator / raw_stk_60_mins`，特征层 `feat_feature_A_price_volume / feat_feature_B_moneyflow / feat_feature_C_chip / feat_feature_D_fundamental / feat_feature_E_intraday_summary`，以及约束层 `raw_stk_limit / raw_suspend_d / raw_index_member_all / raw_index_daily_basic_circ_mv`；同时不同数据起始时间并不一致，日行情与估值可追到 2005，资金流和 60 分钟数据从 2010 开始，筹码从 2018 开始，这一点我也写进了提示词里，要求 AI 在文档中明确分阶段实验边界。

***

## 提示词 1：用于生成《个股 Alpha 模型训练与评估工程文档》

你是一位顶级量化研究员、量化工程负责人和交易系统架构师。请你编写一份**可以直接指导 Python 项目落地**的《个股 Alpha 模型训练与评估工程文档》。注意：你输出的不是泛泛而谈的研究说明，而是一份能让工程师、研究员和后续 AI 编码助手据此直接开始实现项目的**高密度工程文档**。文档必须站在 A 股多域特征截面选股系统的视角撰写，面向“非原始数据，而是已有预处理后的特征层和基础数据层”的环境。

### 一、文档目标与边界

这份文档只解决一个主问题：**如何从已有数据和特征中，训练出稳定、可复现、可评估的个股 Alpha 分数** **`alpha_score`**。\
它只负责“预测层”，**不负责**组合优化、下单执行、实时增强、人工干预。\
必须明确文档输入、输出、边界：

- 输入：基础数据表、预处理后特征表、股票池规则、标签定义规则、训练配置。
- 输出：每日个股 `alpha_score`、训练好的模型对象、模型元数据、评估报告、特征重要性与稳定性分析。
- 不负责：目标权重生成、组合优化器、交易指令生成、回测成交、实盘增强逻辑。

### 二、必须嵌入的数据背景（按已知事实写入文档）

文档必须把以下数据背景写清楚，并把它们纳入工程设计约束：

1. 基础交易层至少包含：
   - `raw_daily`：`ts_code, trade_date, open, high, low, close, pre_close, change, pct_chg, vol, amount`
   - `raw_daily_basic`：换手、量比、估值、市值、流通股本等
   - `raw_moneyflow`：分大中小单资金流买卖量/额、净流入
   - `raw_cyq_perf`：筹码成本分布、加权成本、胜率
   - `raw_fina_indicator`：公告日、报告期、盈利能力、成长、偿债、现金流、周转等大量基本面字段
   - `raw_stk_60_mins`：60 分钟 OHLCV/amount
2. 预处理特征层至少包含 5 个 domain：
   - `feat_feature_A_price_volume`
   - `feat_feature_B_moneyflow`
   - `feat_feature_C_chip`
   - `feat_feature_D_fundamental`
   - `feat_feature_E_intraday_summary`
3. 约束层/辅助层至少包含：
   - `raw_stk_limit`：涨跌停板价格
   - `raw_suspend_d`：停复牌信息
   - `raw_index_member_all`：申万/行业成员关系
   - `raw_index_daily_basic_circ_mv`：行业、市值、名称、市场、上市日期等
4. 数据时间边界必须被建模系统严肃对待：
   - 日行情与估值类数据覆盖很长区间；
   - 资金流与 60 分钟数据从 2010 年附近开始；
   - 筹码相关数据从 2018 年开始；
   - 因此文档必须要求训练和实验系统支持**分起始时间、分特征子集、分模型版本**的回测/训练配置。

### 三、文档必须回答的核心问题

请按工程化方式完整回答以下问题：

1. **训练样本如何构造**\
   包括股票池定义、日期切片、特征对齐、标签可得性、横截面样本组织、缺失处理、异常值处理、行业/市值/风格中性化、停牌/涨跌停/上市未满 N 天过滤逻辑。
2. **标签如何定义**\
   必须给出多套标签方案并推荐主标签：
   - 未来 1D / 5D / 10D 原始收益
   - 截面排序标签
   - 去市场/行业/风格后的残差收益标签
   - top-k 命中型分类标签\
     必须明确标签和交易时点的一致性，不能出现未来信息泄露。
3. **模型如何训练**\
   文档必须要求先建立稳健基线，再考虑复杂模型：
   - 基线：Linear / Ridge / ElasticNet / LightGBM / XGBoost
   - 可扩展：Mamba、Transformer、MoE（但仅作为后续扩展，不得一开始抢占主干）
   - 必须使用 walk-forward / rolling / purged time split
   - 必须支持单域模型、全域模型、分域模型、两层融合模型
4. **模型输出什么**\
   要求输出标准统一：
   - `raw_score`
   - `rank_score`
   - `zscore`
   - 可选 `probability`
   - 每日每股置信度或质量标记
5. **如何评估与验证**\
   至少包括：
   - RankIC / ICIR
   - 分层收益、top-decile vs bottom-decile
   - 不同年份、牛熊震荡阶段表现
   - 不同行业、不同市值组表现
   - 特征重要性漂移
   - 模型漂移监控指标
   - 稳健性：不同训练窗、不同宇宙、不同成本前提下的一致性

### 四、文档必须给出的工程输出结构

要求 AI 生成的工程文档中，必须包含：

1. **项目目录树**
   - `data_contracts/`
   - `labels/`
   - `datasets/`
   - `models/`
   - `training/`
   - `evaluation/`
   - `artifacts/`
   - `configs/`
   - `tests/`
2. **关键接口设计**
   - `FeatureLoader`
   - `UniverseBuilder`
   - `LabelBuilder`
   - `TrainDatasetBuilder`
   - `AlphaModel`
   - `Trainer`
   - `Predictor`
   - `EvaluationRunner`
3. **关键数据对象**
   - `FeatureSnapshot`
   - `LabelFrame`
   - `ModelArtifact`
   - `PredictionFrame`
   - `TrainingReport`
4. **配置系统设计**
   - YAML/JSON 配置
   - 支持按实验区间、特征集、标签集、模型类型切换
5. **日志与可复现设计**
   - 固定随机种子
   - 版本号
   - 数据快照 ID
   - 模型参数记录
   - 训练日志、评估日志、异常日志

### 五、必须强调的稳健性与测试要求

文档必须专门写一章“稳健性与测试”：

- 单元测试：标签构建、样本过滤、时间对齐、泄露检测
- 集成测试：从数据读取到模型输出的一条链路
- 回归测试：同一版本在固定快照下结果一致
- 防泄露测试：`ann_date` 与 `trade_date` 对齐、未来收益错位检查
- 数据质量测试：缺失率突变、异常值突变、截面覆盖率变化
- 监控测试：模型输出分布是否漂移、重要特征是否集体失真

### 六、必须体现的专业判断

文档要明确写出以下观点：

- 这不是原始字段工程，而是“在已有预处理特征基础上做预测系统设计”
- 先做稳健表格模型基线，再做深度模型扩展
- 分域建模优于一开始全部塞进单一黑箱
- 标签定义和交易时点一致性比模型名词更重要
- Alpha 文档不得越界写组合优化与执行细节
- 文档中必须显式区分：研究输出 `alpha_score` ≠ 交易指令

### 七、输出形式要求

请以**正式工程文档 Markdown**格式输出，必须有：

- 文档摘要
- 目标与边界
- 数据契约
- 标签设计
- 训练架构
- 模型设计
- 评估设计
- 测试设计
- 项目结构
- 关键接口
- MVP / 增强版 / 远期版路线图
- 风险与常见错误清单
- 交付清单\
  整篇文档要求极其具体，能直接指导 Python 工程落地，避免空话、避免只讲原理不讲结构。\
  这份提示词所依赖的数据与拆分原则来自我当前已有的 A 股项目背景：采用多域特征、按“预测→组合→执行→增强”拆分，且必须考虑不同数据源起始时间不同这一事实。

***

## 提示词 2：用于生成《Alpha 信号融合、组合优化与风险约束工程文档》

你是一位顶级量化组合经理、风险模型负责人和量化工程架构师。请你编写一份**可直接指导 Python 项目实现**的《Alpha 信号融合、组合优化与风险约束工程文档》。这份文档不是研究论文，也不是抽象投资框架，而是一份让工程师和后续 AI 代码生成器能够直接据此实现组合层系统的正式工程文档。

### 一、文档目标与边界

这份文档只解决一个主问题：**如何把个股** **`alpha_score`** **转化为可持有、可约束、可解释、可复现的目标组合** **`target_portfolio`**。\
它只负责“组合层与风险层”，**不负责**模型训练细节、不负责成交模拟、不负责实时监控和人工干预。

必须明确：

- 输入：每日个股 `alpha_score`、风险暴露、行业信息、市值信息、流动性信息、可交易性约束、可选市场状态信号。
- 输出：`target_weight / target_position / rebalance_list / risk_report`
- 不负责：重新训练 Alpha 模型；不负责订单撮合；不负责回测成交；不负责盘中增强。

### 二、文档必须嵌入的数据与约束背景

请在文档中显式写入并基于它们设计接口：

1. 行业和股票元数据来自 `raw_index_member_all` 与 `raw_index_daily_basic_circ_mv`，可用于行业层级、名称、市场板块、上市日期、市值等映射。
2. 交易约束来自 `raw_stk_limit` 与 `raw_suspend_d`，用于识别涨跌停与停复牌可交易性。
3. 风险和规模代理可来自：
   - `raw_daily_basic` 的 `total_mv / circ_mv / free_share / turnover_rate / volume_ratio / pe_ttm / pb / ps_ttm / dv_ttm`
   - `feat_feature_D_fundamental` 中估值 rank、规模 rank、压缩估值分数等
4. Alpha 来源不只一种：
   - 可来自单一模型
   - 可来自多域模型（A/B/C/D/E）
   - 可来自多 horizon 模型
   - 可来自不同版本模型融合
5. 文档必须承认数据边界差异：基础层、资金流层、筹码层、日内层起始时间不同，因此组合层必须支持“当某类 alpha 缺席时退化运行”的降级机制。

### 三、文档必须回答的核心问题

1. **Alpha 如何融合**\
   文档必须要求给出至少 4 类融合框架：
   - 单模型直接使用
   - 多模型加权平均
   - 多域打分融合（A/B/C/D/E 域）
   - 分层融合：先域内、再域间\
     必须要求解释每种融合适用于什么场景，并给出工程接口。
2. **如何区分 Alpha、Beta、Risk**\
   这部分必须写得非常硬：
   - Alpha：个股相对收益预测
   - Beta：市场方向/总仓位/指数风险预算
   - Risk：行业、风格、市值、流动性、集中度、换手等约束\
     文档必须要求系统支持这三者解耦，而不是混成一个黑箱分数。
3. **top-k 纯多头如何构造**\
   至少要求设计以下版本：
   - topK 等权
   - topK 分数加权
   - topK + buffer
   - topK + 行业/风格/单票约束
   - topK + 二次优化器（risk-adjusted objective）
4. **组合优化器如何设计**\
   文档必须要求给出明确的目标函数和约束：
   - 最大化 alpha 收益
   - 惩罚风险暴露
   - 惩罚换手成本
   - 单票权重上限
   - 行业偏离上限
   - 风格偏离上限
   - 流动性占比约束
   - 总仓位约束
   - 候选池和替补池机制
5. **指数/市场状态信号放在哪**\
   文档必须明确：\
   指数预测可以作为组合层的**仓位调节子模块**，输出 `gross_exposure_scale / cash_ratio_signal / risk_on_off_signal`，但不能回流污染个股 Alpha 模型训练逻辑。
6. **风险约束与 Barra-like 暴露如何接入**\
   必须要求文档设计：
   - 行业风险
   - 市值/流动性/估值/波动/动量等风格代理暴露
   - 主动风险预算
   - 跟踪误差或偏离基准限制
   - 风险归因与事后暴露报告

### 四、必须给出的工程架构与接口

文档必须输出一套能直接指导 Python 项目实现的架构，至少包括：

- `AlphaCombiner`
- `CandidateSelector`
- `RiskExposureBuilder`
- `ConstraintBuilder`
- `PortfolioOptimizer`
- `WeightPostProcessor`
- `PortfolioExporter`
- `RiskReporter`

必须定义标准输入输出对象：

- `AlphaFrame`
- `CompositeAlphaFrame`
- `CandidateUniverse`
- `RiskExposureFrame`
- `ConstraintSet`
- `TargetPortfolio`
- `PortfolioRiskReport`

必须要求设计目录结构，例如：

- `signal_fusion/`
- `candidate_selection/`
- `risk_model/`
- `constraints/`
- `optimizer/`
- `postprocess/`
- `reporting/`
- `configs/`
- `tests/`

### 五、必须强调的专业原则

文档必须强制写入以下原则：

- 组合层只接收预测层输出，不反向决定标签与训练
- 组合层要对“可交易性”敏感，但不去模拟成交
- Alpha 高不等于仓位一定高，仓位由风险与约束共同决定
- 必须区分“研究评分”“组合目标权重”“实际成交持仓”
- 必须支持简单基线和复杂优化器并存，避免过早过度优化
- 必须支持 domain 失效时的降级机制，例如资金流域不可用、筹码域起始较晚时仍能运行

### 六、测试与稳健性要求

文档必须专设“测试与稳健性”章节：

- 单元测试：融合权重、约束构建、优化器输入合法性
- 集成测试：`alpha_score -> target_weight`
- 边界测试：空候选池、行业缺失、市值缺失、不可交易股票激增
- 稳健性测试：不同 K、不同约束强度、不同行业中性强度、不同风险预算
- 压力测试：某一行业整体涨停/停牌、极端低流动性、模型分数集中
- 一致性测试：同一输入快照下目标组合确定性一致

### 七、文档输出形式要求

请输出正式工程文档 Markdown，必须包含：

- 文档摘要
- 目标与边界
- 输入输出契约
- Alpha 融合框架
- 候选池设计
- 组合优化器设计
- 风险模型与约束设计
- 仓位缩放与指数信号子模块
- 质量与测试设计
- 项目目录结构
- 类接口与伪代码级说明
- MVP / 增强版 / 远期版\
  要求内容足够具体，使后续 AI 能直接据此生成可运行的 Python 组合层项目。\
  这份提示词的拆分逻辑和接口边界来自我当前的四文档框架：文档 2 只负责“Alpha 如何变成投资组合”，要求显式区分 Alpha、Beta、Risk，并允许指数信号仅作为组合层仓位调节子模块。

***

## 提示词 3：用于生成《回测执行、持仓管理与交易仿真工程文档》

你是一位顶级量化交易系统工程师、回测引擎设计师和中国 A 股交易规则专家。请你编写一份**能够直接指导 Python 回测与执行仿真系统落地**的《回测执行、持仓管理与交易仿真工程文档》。\
注意：这份文档不是绩效分析综述，也不是简单回测框架介绍，而是面向“真实 A 股约束”的工程文档。它必须能够让工程师、研究员和 AI 编码助手据此构建一套**可信的目标组合→订单→成交→持仓→净值**链路。

### 一、文档目标与边界

这份文档只解决一个主问题：**如何把组合层输出的** **`target_portfolio`** **放进尽可能接近 A 股真实交易环境的仿真系统中验证，并生成执行后持仓和绩效结果**。\
它只负责“执行与回测层”，**不负责**重新训练模型、不负责重新做组合优化、不负责实时增强层的市场状态判断。

输入：

- `target_weight / target_position / rebalance_list`
- 交易日历
- 日频 OHLCV 和 amount
- 60 分钟或更高频辅助执行数据
- 涨跌停数据
- 停复牌数据
- 成本参数、滑点参数、成交规则

输出：

- 订单列表
- 成交回报
- 实际持仓
- 回测净值
- 绩效报告
- 成本与失败交易分析
- 日志与审计轨迹

### 二、必须嵌入的数据背景与交易事实

文档必须把以下数据设计进系统：

1. `raw_daily` 提供 open/high/low/close/pre\_close/vol/amount，是日频成交与估值仿真的基础。
2. `raw_stk_limit` 提供 `up_limit / down_limit`，用于判断涨跌停可成交性。
3. `raw_suspend_d` 提供停复牌状态与可能的盘中停牌时段。
4. `raw_stk_60_mins` 提供 60 分钟 OHLCV/amount，可作为增强执行仿真或择时成交近似。
5. `raw_index_member_all / raw_index_daily_basic_circ_mv` 可辅助做行业、市场、上市日期、名称映射。
6. 文档必须承认：
   - 资金流和分钟数据起始较晚；
   - 筹码数据起始更晚；
   - 回测引擎必须允许“策略逻辑使用哪些上游模块”与“执行仿真层使用哪些可得市场数据”解耦。

### 三、文档必须回答的核心问题

1. **目标组合如何转订单**\
   必须要求设计：
   - 基于目标权重与当前持仓的调仓差
   - 开盘调仓、收盘调仓、盘中固定时点调仓
   - 按手数或最小交易单位取整
   - 现金不足、单票过大、部分成交的处理
2. **成交引擎如何设计**\
   至少要要求提供 3 套成交假设：
   - next open
   - VWAP/amount 近似
   - next close / end-of-day\
     并要求明确每种假设的适用场景与偏差来源。
3. **A 股约束如何进入仿真**\
   必须写入：
   - 涨停买不进
   - 跌停卖不出
   - 停牌不可交易
   - 复牌当日特殊情况要可扩展
   - 上市初期过滤或交易限制可配置
   - ST/特殊风险标的规则可配置
4. **持仓与现金账本如何设计**\
   要求工程文档设计：
   - Position Ledger
   - Cash Ledger
   - Order Book
   - Fill Book
   - Daily NAV State
5. **成本与滑点如何建模**\
   必须设计：
   - 固定手续费
   - 百分比成本
   - 与流动性相关的冲击成本
   - 订单失败和未完成调仓带来的偏差统计
6. **绩效评价与日志系统如何设计**\
   至少包括：
   - 收益、波动、夏普、回撤
   - 换手、成交率、未成交率
   - 成本前后净值对比
   - 行业/风格暴露变化
   - 按日订单日志、成交日志、异常日志
   - 支持多策略并行回测与统一日志聚合

### 四、必须给出的工程架构

文档必须给出面向 Python 的项目结构与接口：

- `BacktestRunner`
- `PortfolioState`
- `OrderGenerator`
- `ExecutionSimulator`
- `FillEngine`
- `LimitSuspensionGuard`
- `CostModel`
- `PerformanceAnalyzer`
- `BacktestLogger`
- `StrategyBridge`（用于接收上游目标组合）

必须定义数据对象：

- `TargetPortfolio`
- `OrderRequest`
- `ExecutedFill`
- `DailyPositionSnapshot`
- `BacktestResult`
- `TradeAuditRecord`

目录建议至少包括：

- `backtest/core/`
- `backtest/orders/`
- `backtest/execution/`
- `backtest/constraints/`
- `backtest/costs/`
- `backtest/performance/`
- `backtest/logging/`
- `tests/`
- `configs/`

### 五、必须强调的工程与研究原则

文档中必须显式写出：

- 回测层不能偷偷改写上游模型和组合逻辑
- 必须区分“目标组合”“订单请求”“实际成交”“期末持仓”
- 不能默认全成交
- 不能忽视涨跌停与停牌
- 不能把回测结果直接等同于实盘，必须输出偏差诊断
- 回测系统必须可解释、可审计、可重放

### 六、测试与稳健性要求

请要求 AI 在文档中专设测试章，至少包括：

- 单元测试：订单生成、涨跌停判断、停牌判断、成本计算
- 集成测试：`target_portfolio -> orders -> fills -> positions -> nav`
- 压力测试：极端大换手、行业集体停牌、连续跌停卖不出
- 回归测试：同一快照和同一参数下回测结果严格一致
- 日志一致性测试：订单数量、成交数量、持仓变更可核对
- 多策略并行测试：多个组合共同运行不互相污染状态

### 七、文档输出形式要求

请输出正式工程文档 Markdown，必须包含：

- 目标与边界
- A 股交易规则映射
- 输入输出契约
- 订单生成逻辑
- 成交仿真逻辑
- 持仓与现金账本
- 成本/滑点模型
- 绩效评价
- 日志与审计
- 项目目录与类接口
- 测试与压力测试
- MVP / 增强版 / 远期版路线图\
  要求内容具体到足以让后续 AI 直接生成 Python 项目骨架和核心模块。\
  这份提示词必须严格遵守我的上位拆分：文档 3 只负责“真实交易环境中的验证”，不反向讨论特征工程、模型选型和 alpha 融合。你的设计要体现这一点。

***

## 提示词 4：用于生成《实时增强、市场状态监控与人工干预工程文档》

你是一位顶级量化研究总监、交易监控系统架构师、实盘风控负责人和 AI 增强交易流程设计专家。请你编写一份**可直接指导 Python 实时增强与监督层系统落地**的《实时增强、市场状态监控与人工干预工程文档》。\
注意：这份文档是整个系统的 **Overlay / Supervisor / Enhancement** 层，而不是主干收益引擎。它的职责不是替代 Alpha、组合和回测主链路，而是在主链路稳定后，对其进行状态识别、风险开关、执行择时增强、事件提示、人工覆盖与模型失效监控。文档必须把这一点写死。

### 一、文档目标与边界

这份文档只解决一个主问题：**在实盘或准实盘环境下，如何利用市场状态、分钟级数据、外部事件与人工判断，对前 3 个主干模块做增强、监督和保护**。\
输入：

- 上游输出：`alpha_score`、`target_portfolio`、订单建议、回测/运行统计
- 市场状态数据、分钟级数据、波动/宽度/情绪指标
- 可选外部事件信息（新闻、公告、宏观事件标签）
- 模型漂移与失效监控指标
- 人工覆盖指令\
  输出：
- 风险开关
- 总仓位缩放因子
- 执行择时增强信号
- 事件风险标签
- 人工审核提示
- 人工覆盖记录
- 模型失效预警
- 保守模式切换信号

不负责：

- 重新训练主模型
- 直接定义基础标签
- 直接替代组合优化器
- 在没有审计记录的情况下强行篡改主干输出

### 二、必须嵌入的数据背景

文档必须基于以下已知数据与事实设计：

1. 你已有日频主干数据、估值/行业信息、资金流、筹码、60 分钟数据和交易约束表。
2. `feat_feature_E_intraday_summary` 已经提供了日内压缩特征，例如 `hour1_ret`、`lasthour_ret`、`midday_reversal`、`intraday_trend_eff`、`hourly_volatility`、`volume_concentration_pm`、`vwap_close_bias_1h`、`high_low_time_skew`，以及多种质量 flag，这使得增强层既可以直接用日内摘要，也可以继续接 5 分钟/15 分钟或 60 分钟序列接口。
3. 分钟/资金流/筹码数据的起始时间不同，因此增强层必须支持“某些增强子模块缺失时自动降级”的运行模式。
4. 这层是增强层，不承担主收益证明责任；文档必须把它设计为可开关、可审计、可回放。

### 三、文档必须回答的 4 大子问题

1. **市场状态识别模块如何设计**\
   必须要求工程文档设计一个 regime 子系统，至少输出：
   - 风险开关
   - 风格环境标签
   - 趋势/震荡分类
   - 高波动/低波动分类
   - 横截面宽度/拥挤度/极端行情标签\
     并明确这些信号如何作用于：
   - 总仓位
   - 换手上限
   - 行业偏离容忍
   - 快/慢 alpha 权重
2. **执行端中频增强模块如何设计**\
   文档必须要求增强层只优先影响“怎么买卖”，而不是“买哪些股票”。\
   必须设计：
   - 基于 60 分钟或更细频率数据的执行时点建议
   - 买卖优先级调整
   - 延迟/提前成交建议
   - 当日流动性不足预警
3. **外部信息增强如何设计**\
   必须把它定义为风险提示优先，而不是直接改仓优先。要求至少支持：
   - 新闻/公告事件标签
   - LLM 情绪打分或风险摘要接口
   - 宏观重大事件标签
   - 组合降权建议与人工审核提示
4. **人工覆盖与模型失效监控如何设计**\
   这是最关键部分之一。文档必须要求：
   - 何时允许人工覆盖
   - 覆盖的权限模型
   - 覆盖后如何记录理由、时间、对象、影响范围
   - 覆盖结果如何纳入事后回放评估
   - 如何检测模型持续失效（IC 崩塌、分数分布漂移、失真特征集体异常、组合表现偏离）
   - 何时切换到保守模式或仅风险控制模式

### 四、必须给出的工程架构与接口

文档必须给出明确 Python 项目架构，至少包括：

- `RegimeDetector`
- `ExecutionOverlay`
- `EventRiskTagger`
- `HumanOverrideManager`
- `ModelDriftMonitor`
- `SafetySwitch`
- `OverlayOrchestrator`
- `AuditRecorder`
- `NotificationDispatcher`

必须定义标准对象：

- `RegimeState`
- `ExecutionOverlaySignal`
- `EventRiskFrame`
- `OverrideRequest`
- `OverrideDecision`
- `DriftAlert`
- `SafetyModeState`

目录结构建议至少包括：

- `overlay/regime/`
- `overlay/execution/`
- `overlay/events/`
- `overlay/drift/`
- `overlay/override/`
- `overlay/audit/`
- `overlay/notifications/`
- `configs/`
- `tests/`

### 五、必须强调的上位原则

文档中必须写死以下原则：

- 这是增强层，不是主干层
- 主干没跑通时，增强层不能接管主逻辑
- 增强层的任何覆盖都必须可审计、可回放、可关闭
- 优先做风险提示，再做直接改仓
- 执行增强优先影响“何时交易”，而非“买哪些股票”
- 人工主观判断必须制度化，而不是拍脑袋
- 模型失效监控必须独立于收益汇报，避免只看净值忽视失真

### 六、测试与稳健性要求

文档必须要求 AI 生成完整测试设计：

- 单元测试：状态识别、阈值判定、覆盖规则、漂移检测
- 集成测试：主干输出接入增强层后不破坏原始数据对象
- 审计测试：每一次覆盖、切换、警报均可追溯
- 降级测试：分钟数据缺失、事件源缺失时系统如何退化运行
- 误触发测试：避免单日噪声导致频繁开关风险模式
- 影子运行测试：增强层先只打标签、不改仓，验证稳定后再逐步生效

### 七、输出形式要求

请输出正式工程文档 Markdown，必须包含：

- 文档摘要
- 目标与边界
- Overlay 层定位
- 市场状态模块
- 执行增强模块
- 外部事件增强模块
- 人工覆盖与审计模块
- 模型失效监控模块
- 安全开关与降级机制
- 项目目录与接口
- 测试与影子运行方案
- MVP / 增强版 / 远期版路线图\
  文档必须足够严格、足够工程化，让后续 AI 能直接生成 Python 增强层项目。\
  这份提示词必须完全遵守我已有的四文档拆分原则：前三层是主干，第四层是增强；第四层要写成 Overlay / Supervisor / Enhancement，而不是主策略主干。

***

这 4 份提示词已经按你的拆分文档和数据现状做了强化：既保留“每份文档只回答一个主问题、必须定义输入输出边界、必须有 MVP/增强/远期”的上位规则，也把 A 股数据表、特征域、交易约束和时间边界写进了提示词本体。

你下一步最合适的是让我继续把这 4 份提示词再升级成“**严格约束 AI 输出格式的终极版**”，也就是每份提示词都附上固定章节模板、必出目录树、必出接口表、必出测试清单和禁止项。
